{
  "buildCommand": "npx prisma generate && npx prisma db push --accept-data-loss && rm -rf .next && npx next build",
  "crons": [
    {
      "path": "/api/cron/abandoned-cart",
      "schedule": "0 3 * * *"
    },
    {
      "path": "/api/cron/auto-cancel",
      "schedule": "0 2 * * *"
    }
  ]
}
