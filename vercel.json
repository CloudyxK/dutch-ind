{
  "buildCommand": "npx prisma generate && npx prisma db push --accept-data-loss && rm -rf .next && mkdir -p .next/server && printf '{\"version\":1,\"files\":[]}' > .next/server/middleware.js.nft.json && npx next build",
  "crons": [
    {
      "path": "/api/cron/abandoned-cart",
      "schedule": "0 3 * * *"
    },
    {
      "path": "/api/cron/auto-cancel",
      "schedule": "0 2 * * *"
    }
  ]
}
